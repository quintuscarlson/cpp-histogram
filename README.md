# cpp-histogram
A C++ histogram tool that loads integer data from files, counts frequencies, and computes statistics including min, max, mode, median, mean, and variance. Includes operator overloading and a demo program.
